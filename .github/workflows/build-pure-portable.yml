name: Build Pure Portable (Windows)

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-pure-windows-portable:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build Pure portable artifact
        run: npm run build:pure:portable

      - name: Upload artifact (dist)
        uses: actions/upload-artifact@v4
        with:
          name: casino-vouchers-pure-portable
          path: dist/**
          if-no-files-found: error
