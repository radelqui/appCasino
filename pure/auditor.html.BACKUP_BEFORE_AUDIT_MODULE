<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Auditoría</title>
    <link rel="stylesheet" href="./style.css" />
    <style>
      .auditor-container { max-width: 1100px; margin: 24px auto; padding: 16px; }
      .auditor-header { display: flex; align-items: center; justify-content: space-between; margin-bottom: 16px; }
      .auditor-title { font-size: 20px; font-weight: 600; }
      .auditor-actions { display: flex; gap: 8px; }
      .btn { padding: 8px 12px; border-radius: 6px; border: 1px solid #ccc; background: #fff; cursor: pointer; }
      .btn.primary { background: #0d6efd; color: #fff; border-color: #0d6efd; }
      .auditor-card { background: #fff; border: 1px solid #e5e7eb; border-radius: 10px; padding: 12px; box-shadow: 0 1px 2px rgba(0,0,0,.06); }
      .muted { color: #6b7280; }
    </style>
  </head>
  <body>
    <div class="auditor-container">
      <div class="auditor-header">
        <div class="auditor-title">Auditoría</div>
        <div class="auditor-actions">
          <button class="btn" id="btnVolver">Volver al Panel</button>
        </div>
      </div>

      <div class="auditor-card">
        <p class="muted">Vista de auditoría básica. Integraremos filtros, totales y exportación aquí sin afectar el diseño del Panel.</p>
      </div>
    </div>

    <script>
      // Volver al panel cerrando esta ventana
      document.getElementById('btnVolver').addEventListener('click', () => {
        window.close();
      });

      // Sistema de salud anti-cuelgues: cargar indicador visual
      (async function cargarIndicadorSalud() {
        try {
          const response = await fetch('health-indicator.html');
          const html = await response.text();
          const tempDiv = document.createElement('div');
          tempDiv.innerHTML = html;
          // Insertar todos los elementos del indicador en el body
          while (tempDiv.firstChild) {
            document.body.appendChild(tempDiv.firstChild);
          }
          console.log('✅ Indicador de salud cargado');
        } catch (e) {
          console.warn('❌ No se pudo cargar indicador de salud:', e.message);
        }
      })();
    </script>
  </body>
  </html>

